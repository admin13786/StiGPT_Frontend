# player-app

玩家端前端工程。项目统一启动说明以 `frontent/README.md` 为准。

真实访问地址：

- `http://localhost:20102`
- 默认产品入口: `/apps`
- 科创 GPT 工作台入口: `/apps/ai-hub`
- 科创 GPT 问答入口: `/apps/stigpt/webIdx`

局部开发可以在本目录运行：

```bash
npm run dev
```
